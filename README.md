The invoice manager project is my IB CS IA and a starting point for a bigger management system.
This project is made with FastAPI

NOTE: the api endpoints / data objects that end in "acname" are only for front-end test development. the optimised data flow of the program does not
recommended using account name to post invoices as it put extra pressure on the back-end while all neede data is optimally already loaded on the front-end
Additionaly these endpoints lack any sort of input correction or validation thus disable them in production

